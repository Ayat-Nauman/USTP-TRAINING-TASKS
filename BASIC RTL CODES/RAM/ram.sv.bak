module ram(
input logic [4:0]addr,
input logic [3:0]data_in,
input logic write,
input logic clk,
output logic [3:0]data_out
);

logic [4:0] ram [31:0];
always_ff @(posedge clk) begin
	if(write)
		ram[addr] <= data_in;
	else 
		data_out <= ram[addr];
end

endmodule
