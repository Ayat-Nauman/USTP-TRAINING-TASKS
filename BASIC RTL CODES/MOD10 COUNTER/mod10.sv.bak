// mod 10 counter
module counter(
	input logic clk, 
	input logic rst, 	  // Assynchronous Reset 
	output logic [9:0] count
	);
	
	always_ff @(posedge clk or posedge rst) begin
		if (rst) 
			count <= 10'b0000;
		else 
			count <= count + 1'b1;	
	end
	
	endmodule
	
